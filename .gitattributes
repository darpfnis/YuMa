# Встановити поведінку за замовчуванням для всіх текстових файлів
* text=auto

# Явно вказати LF для файлів, де це критично (якщо потрібно)
# Наприклад, для скриптів
# *.sh text eol=lf
# *.js text eol=lf
# *.json text eol=lf
# *.css text eol=lf
# *.html text eol=lf